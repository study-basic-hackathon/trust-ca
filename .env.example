# Docker Composeがホスト側に公開するポート。
# 既存の別プロセス/別プロジェクトのコンテナと衝突する場合はここを変更する。
# コンテナ間通信(frontend→backend→db)はDocker内部のポート固定なので影響を受けない。
DB_PORT=5432
BACKEND_PORT=8080
FRONTEND_PORT=3000

# PSA Public API MVP。実際の値はローカルの.envだけに置く。
PSA_MVP_ENABLED=false
PSA_API_TOKEN=
