# backendのベースURL(サーバーサイドfetch用)。
# Docker Compose経由で起動する場合は http://backend:8080 に上書きされる。
BACKEND_URL=http://localhost:8080

# ブラウザからbackendへ直接fetchするための公開URL。
# PSA APIトークン等の秘密情報は絶対に指定しない。
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080
