import type { NextConfig } from "next";

// Server-rendered (Firebase App Hosting). Deliberately not output: "export" —
// see docs/design/system-architecture.md 9節.
const nextConfig: NextConfig = {};

export default nextConfig;
